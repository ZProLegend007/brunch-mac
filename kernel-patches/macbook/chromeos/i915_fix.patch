diff --git a/drivers/gpu/drm/i915/display/intel_fb.c b/drivers/gpu/drm/i915/display/intel_fb.c
index 8af7819db..022a576ab 100644
--- a/drivers/gpu/drm/i915/display/intel_fb.c
+++ b/drivers/gpu/drm/i915/display/intel_fb.c
@@ -1900,6 +1900,7 @@ static int intel_plane_atomic_check_with_state(struct drm_plane *plane,
        if (fb->format->num_planes > 1 && fb->modifier != DRM_FORMAT_MOD_INVALID)
                intel_plane->base.normalized_zpos |= INTEL_PLANE_HAS_PLANE;

+       /* Additional code in intel_plane_atomic_check_with_state function */
+       /* ... (context) */
        return 0;
 }

 static int intel_plane_atomic_check(struct drm_plane *plane,
@@ -1912,7 +1913,8 @@ int intel_framebuffer_init(struct intel_framebuffer *intel_fb,
        if (!drm_any_plane_has_format(&dev_priv->drm,
                                      mode_cmd->pixel_format,
-                                     mode_cmd->modifier[0])) {
+                                     mode_cmd->modifier[0]) &&
+            (mode_cmd->pixel_format != DRM_FORMAT_ARGB8888)) {
            drm_dbg_kms(&dev_priv->drm,
                        "unsupported pixel format %p4cc / modifier 0x%llx\n",
                        &mode_cmd->pixel_format, mode_cmd->modifier[0]);
            goto err;
        }
